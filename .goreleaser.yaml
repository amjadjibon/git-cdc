version: 2

project_name: git-cdc

builds:
  - id: git-cdc
    builder: rust
    binary: git-cdc
    command: zigbuild
    flags:
      - --release
      - -p=git-cdc-cli
    targets:
      - x86_64-unknown-linux-gnu
      - aarch64-unknown-linux-gnu
      - x86_64-apple-darwin
      - aarch64-apple-darwin
# The server ships as a container image (see Containerfile + the release
# workflow's image job), not as release binaries.

archives:
  - id: default
    formats: [tar.gz]
    files:
      - README.md
      - .gitconfig.cdc

checksum:
  name_template: checksums.txt

changelog:
  use: github
  groups:
    - title: Features
      regexp: "^feat"
      order: 0
    - title: Fixes
      regexp: "^fix"
      order: 1
    - title: Other
      order: 999

release:
  draft: false
